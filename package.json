{
  "name": "react-core-common",
  "version": "1.0.117",
  "description": "",
  "main": "./index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup -c && npm version patch && cp package.json ./build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^0.62.0",
    "rollup-plugin-babel": "^3.0.7"
  }
}
